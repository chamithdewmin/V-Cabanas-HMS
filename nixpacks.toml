# Use frontend directory for build (monorepo)
[phases.install]
cmds = ["cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "cd frontend && npx --yes serve -s dist -l 3000"
